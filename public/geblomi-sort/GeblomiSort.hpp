/**
 * GeblomiSort — product header (GEB-PACK-001 restore)
 * ====================================================
 * Full self-contained amalgam (~83 KB) with pdqsort + ska_sort +
 * geblomi hybrid routing lives at attachments/GeblomiSort.hpp.
 * This path is the advertised include target for demos and the storefront.
 *
 * Usage:
 *   #include "GeblomiSort.hpp"
 *   geblomi::sort(begin, end);
 */
#pragma once
#include "../../attachments/GeblomiSort.hpp"
